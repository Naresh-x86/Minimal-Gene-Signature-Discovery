numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.13.0
joblib>=1.3.0
tqdm>=4.65.0
requests>=2.28.0
jupyter>=1.0.0
ipykernel>=6.0.0

# ── PyTorch (GPU) ──────────────────────────────────────────────────────────────
# Install PyTorch separately with the correct CUDA version for your system.
# For CUDA 12.1 (RTX 4060 / recent drivers):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Check your CUDA version with: nvidia-smi
